
// eslint-disable-next-line react/prop-types
const SectionHeader = ({children}) => {
  return (             
    <h2 className=" mt-6 text-base sm:text-xl md:text-3xl lg:text-4xl font-semibold  text-center mb-10  "> {children}</h2>
  )
}

export default SectionHeader