
// eslint-disable-next-line react/prop-types
const SectionHeader = ({children}) => {
  return (             
    <h2 className=" mt-6 text-4xl font-semibold  text-center mb-10  "> {children}</h2>
  )
}

export default SectionHeader